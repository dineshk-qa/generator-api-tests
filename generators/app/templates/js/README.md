# POC for API Integration tests 

This POC demonstrate http endpoint testing with the use of supertest & jest modules


### To Run Tests
`npm install` to install dependencies

`npm test:api:sandbox` to run Api integration tests
